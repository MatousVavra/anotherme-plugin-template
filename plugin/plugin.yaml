name: hello
version: 0.1.0
description: Template hello-world plugin for AnotherMe
author: Your Name
entrypoint: plugin
capabilities:
  - routes
dependencies: []
# Tables this plugin owns (requires the `storage` capability). Writes are
# limited to these; reads stay open to all tables. Omit for legacy
# unrestricted access, or use [] for "no own tables" (key-value storage only).
tables: []
